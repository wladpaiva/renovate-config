# renovate-config

Shared Renovate presets for `wladpaiva` repositories.

## Usage

Use the default preset from another repository with:

```json
{
  "extends": ["github>wladpaiva/renovate-config"]
}
```
